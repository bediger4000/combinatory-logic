# Ensure that the reduction of common sub-graphs still
# happens.  M-combinator used to get the same I-nodes
# into the repeating expression
trace on
count 9
# All 3 (I I I) sub-expressions will reduce in the
# same "step", since we set them up as reference-
# counted common sub-graphs.
M (S T (I I I))
